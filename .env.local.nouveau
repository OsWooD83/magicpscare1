# ========================================
# VARIABLES D'ENVIRONNEMENT - DÉVELOPPEMENT LOCAL
# tw-pascal-nouveau (local)
# ========================================

# === ENVIRONNEMENT ===
NODE_ENV=development
PORT=3000
VERCEL=0

# === API CONFIGURATION ===
# URL locale pour le développement
NEXT_PUBLIC_API_URL=http://localhost:3000/api
API_BASE_URL=http://localhost:3000/api

# === SÉCURITÉ ===
# Secrets pour le développement local
SESSION_SECRET=dev_secret_local_2025
JWT_SECRET=jwt_dev_secret_local_2025

# === CORS CONFIGURATION ===
# CORS plus permissif en développement
CORS_ORIGIN=http://localhost:3000,http://127.0.0.1:3000
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:8080

# === BASE DE DONNÉES ===
# Configuration locale
DATABASE_URL=
MONGODB_URI=mongodb://localhost:27017/tw_pascal_dev
SQLITE_DB_PATH=./data/dev_database.db

# === UPLOAD ET STOCKAGE ===
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=50485760
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif,image/webp,video/mp4

# === EMAIL (développement) ===
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=dev@tw-pascal.local

# === DEBUG ===
DEBUG=true
LOG_LEVEL=debug
